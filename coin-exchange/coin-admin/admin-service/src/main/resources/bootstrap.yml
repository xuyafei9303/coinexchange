spring:
  cloud:
    nacos:
      server-addr: nacos-server:8848
      config:
        file-extension: yaml
  application:
    name: admin-service
  profiles:
    active: dev # 开发环境
# 配合文件的dataId：nacos-server:8840/admin-service-dev.yaml